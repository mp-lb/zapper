project: zapper
env-files:
  - .env.local
  - .env
native:
  dev:
    cmd: pnpm dev
    env: "*"
tasks:
  setup:
    cmds:
      - pnpm install
  build:
    cmds:
      - pnpm build
  test:
    cmds:
      - pnpm test
  lint:
    cmds:
      - pnpm lint
  compile-agents:
    cmds:
      - npx -y @mp-lb/mdcompile CLAUDE.md -o AGENTS.md --optional --silent
