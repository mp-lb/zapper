---
title: "Zapper"
description: "A lightweight dev environment runner for local multi-service projects."
---
A lightweight dev environment runner for local multi-service projects.

## Install

```bash
npm install -g @mp-lb/zapper
```

On Windows, run Zapper from WSL2. Use Linux paths in `zap.yaml`, and make sure
Docker is reachable from that WSL distro before starting Docker services.

## Create `zap.yaml`

```yaml
project: myapp
env: [.env]

native:
  backend:
    cmd: pnpm dev
    env: "*"

  frontend:
    cmd: pnpm dev
    cwd: ./frontend
    env: "*"

docker:
  postgres:
    image: postgres:15
    ports:
      - 5432:5432
```

## Run

```bash
zap up
zap status
zap down
```

## Add Tasks

Use tasks for one-off project commands that should share the same local
environment.

```yaml
tasks:
  seed:
    preconditions:
      - test -n "$DATABASE_URL"
    cmds:
      - pnpm db:seed

  console:
    interactive: true
    silent: true
    cmds:
      - psql "$DATABASE_URL"
```

```bash
zap task seed
zap task console
```

## Deploy

The same `zap.yaml` can deploy to the cloud: add an opt-in `deploy:` block and
run `zap arc deploy` — your services, on your own cloud accounts. See
[Deploy (Zap Arc)](/docs/arc).

For full reference docs, see [Commands](/docs/commands),
[Configuration](/docs/configuration), [Services](/docs/services), and
[Tasks](/docs/tasks).

For profile-based env files, service selection, and isolated stacks, see
[Profiles](/docs/profiles).

For readiness waits and dependency startup behavior, see
[Health Checks](/docs/healthchecks).

For packaging and local machine runtime plans, see
[Local Runtime Compatibility](/docs/local-runtime).

For the command result and rendering contract, see
[Command Output](/docs/output).

For CLI development, testing, and release workflow, see
[CLI Development](https://github.com/mp-lb/zapper/blob/main/docs/tech/cli-development.md)
in the repo.

For the local menu bar app development loop, see
[macOS Development](https://github.com/mp-lb/zapper/blob/main/docs/tech/macos-development.md)
in the repo.
